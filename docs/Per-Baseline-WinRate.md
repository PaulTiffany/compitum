# Per-Baseline Win Rate (Standalone)

This page is populated from evaluation artifacts produced by the bounded RouterBench runs.

If you see this placeholder, generate the inputs, then rebuild docs:

```bash
python tools/generate_eval_tables.py
```

Inputs expected:
- `reports/per_baseline_win_rate.json`
- `reports/per_baseline_agreement.json`

Note: RouterBench sweeps are optional and excluded from default CI gates; this page may be empty until you run the evidence pipeline locally.
